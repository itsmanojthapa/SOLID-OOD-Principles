# LSP: Liskov Substitution Principle

- Subtype objects should be substitutable for supertype objects.
- a child elements sould be easily swappable for the same element type.

- LSP is for making sure that we can easily swap some elements with
  other elements for the same type witin a subtype componenet.
